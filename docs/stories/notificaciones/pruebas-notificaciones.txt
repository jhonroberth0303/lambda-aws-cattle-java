Activa el TTL:
aws dynamodb update-time-to-live --table-name notifications-prod --time-to-live-specification "Enabled=true, AttributeName=ttl" --region us-east-1


Verifica que el request ya funciona (debe dar [], lista vacía, sin error):
curl.exe "https://lakzi3kz3h.execute-api.us-east-1.amazonaws.com/Prod/farms/F001/notifications?limit=30&status=all"


Luego dispara el scheduler para generar la notificación real:

'{"window":"MORNING"}' | Out-File -Encoding ascii payload.json

aws lambda invoke --function-name cattle-notification-scheduler --cli-binary-format raw-in-base64-out --payload file://payload.json --region us-east-1 response.json

Get-Content response.json

created: 1 → notificación creada, revisa la campana en el front.
drafts: 0 → no hay vacas lactantes sin ordeño AM hoy (correcto). Para probar con una real, deja una vaca sin registrar el AM y vuelve a invocar (borrando antes el item DEDUPE#... si ya corrió hoy).